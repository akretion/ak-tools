source "http://api.berkshelf.com"

metadata

cookbook 'apt'
cookbook 'ssh_known_hosts', git: 'https://github.com/Jamstah/ssh_known_hosts.git' #use that fork to avoid requiring the partial_search cookbook for now
